using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Outfit : MonoBehaviour
{
    public string _name;
    public GameObject model;
    public Status status;
    
    // maybe on each guy we delete the main part and instantiate a new part
    // and fists
    // or we have cosmeticitem
    // we even keep type and only have body and fists
}
